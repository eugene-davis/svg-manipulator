#!/bin/bash

# Converts the image to PNG and produces a watermarked form in the end
# Example:
# svg-export-wm bits-must-flow

name=$1

hq-image ${name}
composite -dissolve 35% -gravity center /watermark/watermark.png ${name}_copyright.png  wm-${name}.png
convert -sharpen 0x1.2 -resize 30% -quality 2 wm-${name}.png wm-${name}.png